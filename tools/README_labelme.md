# Labelme Zone Annotation

Used to draw polygon zones on `data/raw/zone_frame.jpg`.

Steps:
1. Run `python -m labelme`
2. Open `data/raw/zone_frame.jpg`
3. Draw polygon zones
4. Save JSON as `data/raw/zone_frame.json`
5. Copy JSON to `config/zones_config.json`